namespace Marathon.Server.Infrastructure
{
    public class WebConstants
    {
        public const string Id = "{id}";
        public const string ProjectId = "{projectId}";
        public const string ProjectTeam = "{projectId}/{teamId}";
    }
}
